apiVersion: v1
kind: Service
metadata:
  name: dr-demo
  namespace: app-producao
  labels:
    app: dr-demo
spec:
  type: LoadBalancer
  selector:
    app: dr-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
